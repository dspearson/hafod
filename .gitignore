# Compiled Chez Scheme libraries
src/**/*.so
src/*.so

# Whole-program optimisation files
src/**/*.wpo
src/*.wpo

# Compiled launcher
bin/hafod.so
bin/hafod.wpo
bin/hafod-native
bin/hafod-standalone

# Standalone build intermediates
tools/*.boot
tools/*_data.c
lib/

# Generated binaries/libraries
tools/gen-platform-constants
src/hafod-ffi-helpers.so
src/hafod-ffi-helpers.dylib

# Generated version library (regenerated from git tags each build via
# tools/gen-version.sh). The VERSION file is the tracked fallback.
src/hafod/internal/version.ss

# Generated man page (rendered from doc/hafod.1.in at build time)
doc/hafod.1

# Local platform stamp (records which platform platform-constants.ss is built for)
src/hafod/internal/.platform-stamp

# Build artefacts
a.out
tmpfile

# Nix / direnv
.direnv/
result

# Editor
*~
\#*\#
