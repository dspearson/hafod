name: CI

on:
  push:
    branches: [main, 'v*']
  pull_request:
    branches: [main]

jobs:
  test-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixpkgs-unstable

      - name: Build (compile-wpo)
        run: nix develop --command make compile-wpo

      - name: Run tests
        run: nix develop --command make test

      - name: Build native binary
        run: nix develop --command make native

      - name: Verify native binary
        run: |
          bin/hafod-native --version
          bin/hafod-native -c '(display "ok\n")'

      - name: Build standalone binary
        run: nix develop --command make standalone

      - name: Verify standalone binary
        run: |
          bin/hafod-standalone --version
          bin/hafod-standalone -c '(display "ok\n")'

  test-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixpkgs-unstable

      - name: Generate platform constants
        run: nix develop --command make platform-constants

      - name: Build (compile-wpo)
        run: nix develop --command make compile-wpo

      - name: Run tests
        run: nix develop --command make test

      - name: Build native binary
        run: nix develop --command make native

      - name: Verify native binary
        run: |
          bin/hafod-native --version
          bin/hafod-native -c '(display "ok\n")'

  nix-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixpkgs-unstable

      - name: Build with Nix
        run: nix build

      - name: Verify Nix binary
        run: |
          ./result/bin/hafod --version
          ./result/bin/hafod -c '(display "ok\n")'
